[project]
name = 'Smart-Property-Insurance'
description = ''
authors = []
telemetry = true
cache_dir = '.\.cache'
requirements = []
[contracts.iot-sensor-oracle]
path = 'contracts/iot-sensor-oracle.clar'
clarity_version = 3
epoch = '3.2'

[contracts.risk-based-premium-adjustment]
path = 'contracts/risk-based-premium-adjustment.clar'
clarity_version = 3
epoch = 'latest'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
